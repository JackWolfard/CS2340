const Constants = Object.freeze({
    UNIVERSE_SIZE: Object.freeze({
        MIN: new Vector(150, 100),
        MAX: new Vector(250, 200)
    }),
    SOLARSYSTEM_SIZE: Object.freeze({
        MIN: new Vector(15, 10),
        MAX: new Vector(25, 20)
    }),
    SPECIAL_KEYS: {},
});